@{
    ViewData["Title"] = "Lecturers";
}

<section class="space-y-6 px-1 sm:px-2">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
        <div>
            <p class="texttext-sm font-semibold uppercase tracking-[0.25em] text-brand-700">Lecturer Management</p>
            <h1 class="mt-2 text-2xl font-bold text-slate-800">Lecturer List</h1>
            <p class="mt-2 text-sm text-slate-500">View, filter, and manage registered lecturer records.</p>
        </div>
    </div>

    <div class="rounded-[28px] border border-slate-200 bg-white p-5 shadow-panel sm:p-6">
        <div class="flex flex-col gap-3 border-b border-slate-100 pb-4 sm:flex-row sm:items-center sm:justify-between">
            <div class="flex items-center gap-3">
                <div class="flex h-10 w-10 items-center justify-center rounded-2xl bg-brand-50 text-brand-700">
                    <i class="fa-solid fa-filter"></i>
                </div>
                <div>
                    <h2 class="text-lg font-bold text-slate-900">Filter Lecturers</h2>
                    <p class="text-sm text-slate-500">Search lecturers by department, name, or registration number.</p>
                </div>
            </div>

            <button id="clearFiltersButton" type="button" class="inline-flex items-center justify-center gap-2 rounded-full bg-slate-100 px-4 py-2 text-xs font-bold text-slate-700 transition hover:bg-slate-200">
                <i class="fa-solid fa-rotate-left"></i>
                Clear Filters
            </button>
        </div>

        <form class="mt-5 grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-12">
            <div class="sm:col-span-2 xl:col-span-4">
                <label class="block text-xs font-bold text-slate-700">Department</label>
                <div class="relative mt-2">
                    <button id="departmentDropdownButton" type="button" class="flex w-full items-center justify-between rounded-2xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-semibold text-slate-700 shadow-sm transition hover:border-brand-300 focus:outline-none focus:ring-2 focus:ring-brand-600">
                        <span id="selectedDepartmentText" class="truncate text-slate-400">Search Department</span>
                        <i class="fa-solid fa-chevron-down text-xs text-slate-400"></i>
                    </button>

                    <div id="departmentDropdownPanel" class="absolute z-40 mt-2 hidden w-full overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-2xl">
                        <div class="border-b border-slate-100 p-3">
                            <div class="relative">
                                <i class="fa-solid fa-magnifying-glass absolute left-4 top-3.5 text-sm text-slate-400"></i>
                                <input id="departmentSearchInput" type="text" placeholder="Search department" class="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-4 text-sm focus:outline-none focus:ring-2 focus:ring-brand-600" />
                            </div>
                        </div>
                        <div id="departmentOptions" class="max-h-64 overflow-y-auto p-2"></div>
                    </div>
                </div>
                <input id="selectedDepartmentInput" type="hidden" />
            </div>

            <div class="sm:col-span-1 xl:col-span-3">
                <label for="lecturerNameFilter" class="block text-xs font-bold text-slate-700">Lecturer Name</label>
                <input id="lecturerNameFilter" type="text" placeholder="Search by lecturer name" class="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm font-semibold text-slate-700 focus:outline-none focus:ring-2 focus:ring-brand-600" />
            </div>

            <div class="sm:col-span-1 xl:col-span-3">
                <label for="regNumberFilter" class="block text-xs font-bold text-slate-700">Reg Number</label>
                <input id="regNumberFilter" type="text" placeholder="Search by registration number" class="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm font-semibold text-slate-700 focus:outline-none focus:ring-2 focus:ring-brand-600" />
            </div>

            <div class="flex items-end sm:col-span-2 xl:col-span-2">
                <button id="applyFiltersButton" type="button" class="inline-flex w-full items-center justify-center gap-2 rounded-full bg-brand-700 px-4 py-3 text-xs font-bold text-white shadow-lg shadow-brand-700/20 transition hover:-translate-y-0.5 hover:bg-brand-800 focus:outline-none focus:ring-2 focus:ring-brand-600">
                    <i class="fa-solid fa-filter"></i>
                    Apply Filter
                </button>
            </div>
        </form>
    </div>

    <div class="overflow-hidden rounded-[28px] border border-slate-200 bg-white shadow-panel">
        <div class="flex flex-col gap-3 border-b border-slate-100 px-5 py-5 sm:flex-row sm:items-center sm:justify-between sm:px-6">
            <div class="flex items-center gap-3">
                <div class="flex h-10 w-10 items-center justify-center rounded-2xl bg-brand-50 text-brand-700">
                    <i class="fa-solid fa-chalkboard-user"></i>
                </div>
                <div>
                    <h2 class="text-lg font-extrabold text-slate-900">Lecturers Table</h2>
                    <p id="lecturerCountText" class="mt-1 text-sm text-slate-500">Showing all lecturers</p>
                </div>
            </div>
            <span id="activeFilterBadge" class="hidden rounded-full bg-brand-50 px-4 py-2 text-xs font-extrabold text-brand-700">Filters active</span>
        </div>

        <div id="lecturerTableContainer" class="w-full overflow-x-auto">
            <table class="w-full min-w-[1350px] divide-y divide-slate-200 text-xs sm:text-sm">
                <thead class="bg-slate-50 text-left text-sm font-bold text-slate-700">
                    <tr>
                        <th class="px-3 py-3 sm:px-5 sm:py-4">Title</th>
                        <th class="px-3 py-3 sm:px-5 sm:py-4">Full Name</th>
                        <th class="px-3 py-3 sm:px-5 sm:py-4">Reg No.</th>
                        <th class="px-3 py-3 sm:px-5 sm:py-4">Department</th>
                        <th class="px-3 py-3 sm:px-5 sm:py-4">Status</th>
                        <th class="px-3 py-3 sm:px-5 sm:py-4">Email Address</th>
                        <th class="px-3 py-3 sm:px-5 sm:py-4">Contact Number</th>
                        <th class="px-3 py-3 text-right sm:px-5 sm:py-4">Profile</th>
                        <th class="px-3 py-3 text-right sm:px-5 sm:py-4">Edit</th>
                    </tr>
                </thead>
                <tbody id="lecturerTableBody" class="divide-y divide-slate-100 bg-white"></tbody>
            </table>
        </div>

        <div id="pagination" class="flex flex-wrap items-center justify-center gap-2 border-t border-slate-100 px-5 py-5"></div>

        <div id="noResultsBox" class="hidden px-6 py-12 text-center">
            <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-brand-50 text-brand-700">
                <i class="fa-solid fa-magnifying-glass text-xl"></i>
            </div>
            <h3 class="mt-4 text-lg font-extrabold text-slate-900">No lecturers found</h3>
            <p class="mt-2 text-sm text-slate-500">Try changing the department, lecturer name, or registration number filters.</p>
        </div>
    </div>
</section>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const departments = [
            'Department of Languages',
            'Department of Art & Design',
            'Department of Life Science',
            'Department of Computing',
            'Department of Engineering',
            'Department of Management & Law'
        ];

        const lecturers = [
            { id: 1, title: 'Dr.', fullName: 'Nimal Perera', regNo: 'LEC-2026-001', department: 'Department of Computing', status: 'Active', email: 'nimal.perera@smartedu.lk', contactNumber: '+94 71 245 8891' },
            { id: 2, title: 'Ms.', fullName: 'Thilini Fernando', regNo: 'LEC-2026-002', department: 'Department of Computing', status: 'Active', email: 'thilini.fernando@smartedu.lk', contactNumber: '+94 77 564 2281' },
            { id: 3, title: 'Mr.', fullName: 'Kasun Wijesinghe', regNo: 'LEC-2026-003', department: 'Department of Computing', status: 'Inactive', email: 'kasun.wijesinghe@smartedu.lk', contactNumber: '+94 76 882 7741' },
            { id: 4, title: 'Dr.', fullName: 'Chamara Silva', regNo: 'LEC-2026-004', department: 'Department of Computing', status: 'Active', email: 'chamara.silva@smartedu.lk', contactNumber: '+94 70 334 1128' },
            { id: 5, title: 'Ms.', fullName: 'Dilini Jayawardena', regNo: 'LEC-2026-005', department: 'Department of Computing', status: 'Active', email: 'dilini.jayawardena@smartedu.lk', contactNumber: '+94 75 889 3412' },
            { id: 6, title: 'Mr.', fullName: 'Lahiru Bandara', regNo: 'LEC-2026-006', department: 'Department of Engineering', status: 'Inactive', email: 'lahiru.bandara@smartedu.lk', contactNumber: '+94 78 672 4490' },
            { id: 7, title: 'Dr.', fullName: 'Amila Rathnayake', regNo: 'LEC-2026-007', department: 'Department of Engineering', status: 'Active', email: 'amila.rathnayake@smartedu.lk', contactNumber: '+94 71 908 7762' },
            { id: 8, title: 'Ms.', fullName: 'Kavindi Herath', regNo: 'LEC-2026-008', department: 'Department of Engineering', status: 'Active', email: 'kavindi.herath@smartedu.lk', contactNumber: '+94 76 521 9983' },
            { id: 9, title: 'Mr.', fullName: 'Ruwan De Silva', regNo: 'LEC-2026-009', department: 'Department of Art & Design', status: 'Inactive', email: 'ruwan.desilva@smartedu.lk', contactNumber: '+94 77 301 5528' },
            { id: 10, title: 'Dr.', fullName: 'Harsha Gunawardena', regNo: 'LEC-2026-010', department: 'Department of Art & Design', status: 'Active', email: 'harsha.gunawardena@smartedu.lk', contactNumber: '+94 72 654 4421' },
            { id: 11, title: 'Ms.', fullName: 'Anjali Perera', regNo: 'LEC-2026-011', department: 'Department of Art & Design', status: 'Active', email: 'anjali.perera@smartedu.lk', contactNumber: '+94 71 667 2345' },
            { id: 12, title: 'Dr.', fullName: 'Saman Kumara', regNo: 'LEC-2026-012', department: 'Department of Languages', status: 'Inactive', email: 'saman.kumara@smartedu.lk', contactNumber: '+94 74 213 9981' },
            { id: 13, title: 'Ms.', fullName: 'Hiruni Wickramasinghe', regNo: 'LEC-2026-013', department: 'Department of Languages', status: 'Active', email: 'hiruni.wickramasinghe@smartedu.lk', contactNumber: '+94 76 118 7345' },
            { id: 14, title: 'Mr.', fullName: 'Pradeep Senanayake', regNo: 'LEC-2026-014', department: 'Department of Languages', status: 'Active', email: 'pradeep.senanayake@smartedu.lk', contactNumber: '+94 77 845 6620' },
            { id: 15, title: 'Dr.', fullName: 'Hashini Ekanayake', regNo: 'LEC-2026-015', department: 'Department of Life Science', status: 'Inactive', email: 'hashini.ekanayake@smartedu.lk', contactNumber: '+94 70 984 5521' },
            { id: 16, title: 'Ms.', fullName: 'Chamodi Rathnayake', regNo: 'LEC-2026-016', department: 'Department of Life Science', status: 'Active', email: 'chamodi.rathnayake@smartedu.lk', contactNumber: '+94 71 345 7812' },
            { id: 17, title: 'Dr.', fullName: 'Malith Dissanayake', regNo: 'LEC-2026-017', department: 'Department of Life Science', status: 'Active', email: 'malith.dissanayake@smartedu.lk', contactNumber: '+94 75 674 2201' },
            { id: 18, title: 'Mr.', fullName: 'Pasan Abeysekara', regNo: 'LEC-2026-018', department: 'Department of Management & Law', status: 'Inactive', email: 'pasan.abeysekara@smartedu.lk', contactNumber: '+94 78 432 1198' },
            { id: 19, title: 'Dr.', fullName: 'Yasith Samarasinghe', regNo: 'LEC-2026-019', department: 'Department of Management & Law', status: 'Active', email: 'yasith.samarasinghe@smartedu.lk', contactNumber: '+94 77 926 3814' },
            { id: 20, title: 'Ms.', fullName: 'Sanduni Ranasinghe', regNo: 'LEC-2026-020', department: 'Department of Management & Law', status: 'Active', email: 'sanduni.ranasinghe@smartedu.lk', contactNumber: '+94 70 552 6432' }
        ];

        const departmentDropdownButton = document.getElementById('departmentDropdownButton');
        const departmentDropdownPanel = document.getElementById('departmentDropdownPanel');
        const departmentSearchInput = document.getElementById('departmentSearchInput');
        const departmentOptions = document.getElementById('departmentOptions');
        const selectedDepartmentText = document.getElementById('selectedDepartmentText');
        const selectedDepartmentInput = document.getElementById('selectedDepartmentInput');
        const lecturerNameFilter = document.getElementById('lecturerNameFilter');
        const regNumberFilter = document.getElementById('regNumberFilter');
        const applyFiltersButton = document.getElementById('applyFiltersButton');
        const clearFiltersButton = document.getElementById('clearFiltersButton');
        const lecturerTableBody = document.getElementById('lecturerTableBody');
        const lecturerCountText = document.getElementById('lecturerCountText');
        const activeFilterBadge = document.getElementById('activeFilterBadge');
        const noResultsBox = document.getElementById('noResultsBox');
        const pagination = document.getElementById('pagination');
        const lecturerTableContainer = document.getElementById('lecturerTableContainer');

        const itemsPerPage = 5;
        let currentPage = 1;
        let selectedDepartment = '';
        let currentFilteredLecturers = [];

        function renderDepartmentOptions(keyword = '') {
            const normalizedKeyword = keyword.trim().toLowerCase();
            const filteredDepartments = departments.filter(department => department.toLowerCase().includes(normalizedKeyword));
            departmentOptions.innerHTML = '';

            const allButton = document.createElement('button');
            allButton.type = 'button';
            allButton.className = 'w-full rounded-xl px-4 py-3 text-left text-sm font-bold text-slate-700 transition hover:bg-brand-50';
            allButton.textContent = 'All departments';
            allButton.addEventListener('click', () => selectDepartment(''));
            departmentOptions.appendChild(allButton);

            if (filteredDepartments.length === 0) {
                const emptyItem = document.createElement('div');
                emptyItem.className = 'px-4 py-6 text-center text-sm font-semibold text-slate-400';
                emptyItem.textContent = 'No departments found.';
                departmentOptions.appendChild(emptyItem);
                return;
            }

            filteredDepartments.forEach(department => {
                const button = document.createElement('button');
                button.type = 'button';
                button.className = 'w-full rounded-xl px-4 py-3 text-left text-sm font-bold text-slate-700 transition hover:bg-brand-50';
                button.textContent = department;
                button.addEventListener('click', () => selectDepartment(department));
                departmentOptions.appendChild(button);
            });
        }

        function selectDepartment(department) {
            selectedDepartment = department;
            selectedDepartmentInput.value = department;
            selectedDepartmentText.textContent = department || 'All departments';
            selectedDepartmentText.classList.toggle('text-slate-400', department === '');
            selectedDepartmentText.classList.toggle('text-slate-900', department !== '');
            departmentDropdownPanel.classList.add('hidden');
        }

        function getStatusBadge(status) {
            const statusStyles = {
                Active: 'bg-emerald-50 text-emerald-700 ring-emerald-200',
                Inactive: 'bg-rose-50 text-rose-700 ring-rose-200'
            };
            return `<span class="inline-flex rounded-full px-3 py-1 text-xs font-extrabold ring-1 ${statusStyles[status]}">${status}</span>`;
        }

        function applyFilters(resetPage = true) {
            const lecturerNameKeyword = lecturerNameFilter.value.trim().toLowerCase();
            const regNumberKeyword = regNumberFilter.value.trim().toLowerCase();

            currentFilteredLecturers = lecturers.filter(lecturer => {
                const matchesDepartment = !selectedDepartment || lecturer.department === selectedDepartment;
                const matchesName = !lecturerNameKeyword || lecturer.fullName.toLowerCase().includes(lecturerNameKeyword);
                const matchesRegNo = !regNumberKeyword || lecturer.regNo.toLowerCase().includes(regNumberKeyword);
                return matchesDepartment && matchesName && matchesRegNo;
            });

            if (resetPage) {
                currentPage = 1;
            }

            renderTable(currentFilteredLecturers);
            renderPagination(currentFilteredLecturers.length);
            updateFilterStatus(currentFilteredLecturers.length);
        }

        function renderTable(data) {
            lecturerTableBody.innerHTML = '';
            noResultsBox.classList.toggle('hidden', data.length > 0);

            const startIndex = (currentPage - 1) * itemsPerPage;
            const endIndex = startIndex + itemsPerPage;
            const paginatedData = data.slice(startIndex, endIndex);

            paginatedData.forEach(lecturer => {
                const row = document.createElement('tr');
                row.className = 'transition hover:bg-slate-50';
                row.innerHTML = `
                    <td class="px-3 py-3 font-semibold text-slate-700 sm:px-5 sm:py-4">${lecturer.title}</td>
                    <td class="px-3 py-3 font-semibold text-slate-900 sm:px-5 sm:py-4">${lecturer.fullName}</td>
                    <td class="px-3 py-3 text-slate-600 sm:px-5 sm:py-4">${lecturer.regNo}</td>
                    <td class="px-3 py-3 text-slate-700 sm:px-5 sm:py-4">${lecturer.department}</td>
                    <td class="px-3 py-3 sm:px-5 sm:py-4">${getStatusBadge(lecturer.status)}</td>
                    <td class="px-3 py-3 text-slate-700 sm:px-5 sm:py-4">${lecturer.email}</td>
                    <td class="px-3 py-3 text-slate-700 sm:px-5 sm:py-4">${lecturer.contactNumber}</td>
                    <td class="px-3 py-3 text-right sm:px-5 sm:py-4">
                        <a href="/Lecturer/Profile/${lecturer.id}" class="inline-flex items-center justify-center gap-2 rounded-full bg-brand-50 px-4 py-2 text-xs font-bold text-brand-700 transition hover:bg-brand-100">
                            <i class="fa-solid fa-id-card"></i>
                            Profile
                        </a>
                    </td>
                    <td class="px-3 py-3 text-right sm:px-5 sm:py-4">
                        <a href="/Lecturer/Edit/${lecturer.id}" class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-amber-50 text-amber-700 transition hover:bg-amber-100">
                            <i class="fa-solid fa-pen-to-square"></i>
                        </a>
                    </td>
                `;
                lecturerTableBody.appendChild(row);
            });
        }

        function renderPagination(totalItems) {
            pagination.innerHTML = '';
            const totalPages = Math.ceil(totalItems / itemsPerPage);

            if (totalPages <= 1) {
                return;
            }

            const createButton = (label, page, disabled = false, active = false) => {
                const button = document.createElement('button');
                button.type = 'button';
                button.textContent = label;
                button.disabled = disabled;
                button.className = active
                    ? 'h-10 min-w-10 rounded-full bg-blue-700 px-4 text-sm font-extrabold text-white shadow-md'
                    : 'h-10 min-w-10 rounded-full border border-slate-200 bg-white px-4 text-sm font-bold text-slate-700 transition hover:bg-blue-50 hover:text-blue-700 disabled:cursor-not-allowed disabled:opacity-40';

                button.addEventListener('click', () => {
                    if (!disabled) {
                        currentPage = page;
                        renderTable(currentFilteredLecturers);
                        renderPagination(currentFilteredLecturers.length);
                        lecturerTableContainer.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }
                });

                pagination.appendChild(button);
            };

            createButton('Prev', Math.max(1, currentPage - 1), currentPage === 1);

            for (let page = 1; page <= totalPages; page++) {
                createButton(page, page, false, page === currentPage);
            }

            createButton('Next', Math.min(totalPages, currentPage + 1), currentPage === totalPages);
        }

        function updateFilterStatus(count) {
            const filtersActive = Boolean(selectedDepartment || lecturerNameFilter.value.trim() || regNumberFilter.value.trim());
            activeFilterBadge.classList.toggle('hidden', !filtersActive);
            lecturerCountText.textContent = `${count} lecturer${count === 1 ? '' : 's'} found`;
        }

        function clearFilters() {
            selectedDepartment = '';
            selectedDepartmentInput.value = '';
            selectedDepartmentText.textContent = 'Search Department';
            selectedDepartmentText.classList.add('text-slate-400');
            selectedDepartmentText.classList.remove('text-slate-900');
            departmentSearchInput.value = '';
            lecturerNameFilter.value = '';
            regNumberFilter.value = '';
            renderDepartmentOptions();
            applyFilters(true);
        }

        departmentDropdownButton.addEventListener('click', () => {
            departmentDropdownPanel.classList.toggle('hidden');
            departmentSearchInput.value = '';
            renderDepartmentOptions();
            setTimeout(() => departmentSearchInput.focus(), 0);
        });

        document.addEventListener('click', event => {
            if (!departmentDropdownPanel.contains(event.target) && !departmentDropdownButton.contains(event.target)) {
                departmentDropdownPanel.classList.add('hidden');
            }
        });

        departmentSearchInput.addEventListener('input', event => renderDepartmentOptions(event.target.value));
        applyFiltersButton.addEventListener('click', () => applyFilters(true));
        clearFiltersButton.addEventListener('click', clearFilters);

        lecturerNameFilter.addEventListener('keydown', event => {
            if (event.key === 'Enter') {
                event.preventDefault();
                applyFilters(true);
            }
        });

        regNumberFilter.addEventListener('keydown', event => {
            if (event.key === 'Enter') {
                event.preventDefault();
                applyFilters(true);
            }
        });

        renderDepartmentOptions();
        applyFilters(true);
    });
</script>
